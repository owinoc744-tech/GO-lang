package main

import(
"fmt"
)
func main(){
var name string 
fmt.Print("Enyer your name:")
fmt.Scan(&name)
num := 17
fmt.Printf(" %s Welcome to fsociety  your badge number is %d \n",name,num)
fmt.Println(string.ToUpper(name))
}
